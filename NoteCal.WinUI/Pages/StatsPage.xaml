<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NoteCal_WinUI.Pages.StatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="出勤统计" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{Binding MonthTitle}" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="请假天不计入出勤。" />
            </StackPanel>
            <CommandBar
                AutomationProperties.AutomationId="StatsCommandBar"
                Grid.Column="1"
                DefaultLabelPosition="Right"
                IsOpen="False">
                <AppBarButton AutomationProperties.AutomationId="StatsPreviousMonthButton" Click="PreviousMonth_Click" Icon="Back" Label="上月" />
                <AppBarButton AutomationProperties.AutomationId="StatsCurrentMonthButton" Click="CurrentMonth_Click" Icon="CalendarDay" Label="本月" />
                <AppBarButton AutomationProperties.AutomationId="StatsNextMonthButton" Click="NextMonth_Click" Icon="Forward" Label="下月" />
            </CommandBar>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Text="{Binding AttendanceDays}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="出勤天" />
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Text="{Binding OvertimeDays}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="加班天" />
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Text="{Binding BusinessTripDays}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="出差天" />
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Text="{Binding LeaveDays}" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="请假天" />
                </StackPanel>
            </Border>
        </Grid>

        <Border
            Grid.Row="2"
            Padding="18"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <StackPanel Spacing="10">
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="明细" />
                <Grid ColumnSpacing="14" RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock FontWeight="SemiBold" Text="加班日期" />
                    <TextBlock Grid.Column="1" Text="{Binding OvertimeDates}" TextWrapping="Wrap" />
                    <TextBlock Grid.Row="1" FontWeight="SemiBold" Text="出差日期" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding BusinessTripDates}" TextWrapping="Wrap" />
                    <TextBlock Grid.Row="2" FontWeight="SemiBold" Text="请假日期" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding LeaveDates}" TextWrapping="Wrap" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
